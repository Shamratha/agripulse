# Pinned so the reported metrics (kappa, per-class F1, agreement) are reproducible.
numpy==2.5.0
scikit-learn==1.9.0
matplotlib==3.11.0
fastapi==0.139.0
uvicorn==0.49.0

# Required only for --mode gee (live Google Earth Engine data). Sample mode
# runs without it. After install: `earthengine authenticate`.
earthengine-api==1.7.33

# Test suite
pytest==8.3.4
